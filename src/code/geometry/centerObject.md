
```js
// ----------------------------------------------------------------------------
// Center map on object.  
// ----------------------------------------------------------------------------

Map.centerObject(point_collection, 14);

// Add point_collection object to map as a layer.

Map.addLayer(
  XXXX,                             // data object
  XXXX,                             // display with black color
  XXXX,                             // label 'Point collection'
  X                                 // shown by default should be true
  )
;
```
